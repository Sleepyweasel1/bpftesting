Category: enhancement
Status: completed

# Encode Capture Mode as a typed invariant

## What to build

Replace the raw `u8` `replay` field in `StateEntry` with a `CaptureMode` enum (`repr(u8)`, `PassThrough = 0`, `Hold = 1`) defined in `hold-packet-common`. Update the eBPF classifier and all userspace code (`grpc.rs`, `main.rs` Idle Monitor) to use `CaptureMode` instead of integer literals. The enum must compile under `#![no_std]` for the eBPF target and under normal conditions for the userspace target.

End-to-end: the Idle Monitor flips a Captured IP from `CaptureMode::PassThrough` to `CaptureMode::Hold` after the Idle Timeout; the eBPF classifier checks `CaptureMode::Hold` to decide whether to redirect to the TAP device.

The chosen design is:

- `StateEntry.mode: CaptureMode` is stored directly in the shared kernel/userspace record.
- All in-repo writers are typed and write `CaptureMode` variants, not raw integers.
- Userspace performs a startup validation pass before normal operation and fails fast if map state is detected to be corrupt or incompatible.
- A convenience predicate `CaptureMode::is_hold()` may be used at call sites to make redirect semantics explicit.

This issue does **not** require proving that arbitrary raw bytes from kernel map memory are mathematically unrepresentable as invalid enum discriminants. It requires typed writers plus explicit startup validation behavior.

## Acceptance criteria

- [x] `CaptureMode` `#[repr(u8)]` enum is defined in `hold-packet-common` and compiles for both eBPF (`bpfel`) and host targets.
- [x] `StateEntry.replay` field is replaced by `StateEntry.mode: CaptureMode`; no raw `0`/`1` integer comparisons remain in `grpc.rs`, `main.rs`, or `hold-packet-ebpf/src/main.rs`.
- [x] Userspace performs one startup validation pass over existing state-map entries and hard-fails before normal operation if Capture Mode state is invalid or incompatible.
- [x] All existing unit tests for `calculate_state_updates` pass; tests are updated to use `CaptureMode` variants.
- [x] `CONTEXT.md` glossary entry for `Capture Mode` is updated to reference the `CaptureMode` type.

## Blocked by

None — can start immediately.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

**What we've established so far:**

- Category is `enhancement`.
- This issue is fully specified enough for an AFK agent.
- The intended design stores `CaptureMode` directly in `StateEntry.mode`.
- All writers in the repo should use typed `CaptureMode` variants rather than raw integer literals.
- Userspace should perform a single startup validation pass and fail fast on invalid or incompatible persisted state.
- The scope includes the shared type, eBPF redirect decision, Idle Monitor transitions, tests, and glossary updates.

**What we still need from you (@maintainer):**

- Nothing for scoping. The issue can move to implementation.

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Replace the raw replay byte in the shared capture state with a typed `CaptureMode` enum and update all call sites to use the type consistently.

**Current behavior:**
The shared `StateEntry` record uses a raw byte field to represent the current Capture Mode. The eBPF classifier and userspace Idle Monitor compare and write integer literals to decide whether a Captured IP is in pass-through or hold mode. This leaves the mode semantics implicit and makes the domain invariant easy to drift.

**Desired behavior:**
The shared `StateEntry` record should expose a `CaptureMode` enum with `PassThrough` and `Hold` variants. The eBPF classifier should redirect only when the mode is `Hold`. The Idle Monitor should transition between `PassThrough` and `Hold` using the typed enum. Userspace should perform a one-time startup validation pass over existing map entries and fail fast before normal operation if persisted state is invalid or incompatible with the typed representation.

**Key interfaces:**
- `CaptureMode` — a `#[repr(u8)]` enum in the shared common crate that compiles for both eBPF and host builds
- `StateEntry` — replace the raw replay byte with `mode: CaptureMode`
- Idle Monitor state transition logic — compare and write `CaptureMode` variants rather than integer literals
- eBPF redirect decision — use typed Capture Mode semantics, optionally via an `is_hold()` helper
- Startup validation at daemon boot — validate persisted capture-state entries before normal operation begins

**Acceptance criteria:**
- [ ] `CaptureMode` `#[repr(u8)]` enum exists in the shared common crate and is usable from both eBPF and userspace code.
- [ ] `StateEntry` uses `mode: CaptureMode` and raw `0`/`1` comparisons for Capture Mode are removed from the eBPF classifier and userspace Idle Monitor code.
- [ ] All in-repo writers of Capture Mode state write typed enum variants rather than raw integers.
- [ ] Userspace performs a startup validation pass over persisted state-map entries and hard-fails before normal operation if Capture Mode state is invalid or incompatible.
- [ ] Existing `calculate_state_updates` tests pass after being migrated to `CaptureMode` variants.
- [ ] The glossary entry for Capture Mode references the `CaptureMode` type.

**Out of scope:**
- Changing Idle Timeout semantics, polling cadence, or other state-transition rules unrelated to type representation
- Refactoring unrelated warnings or cleanup in replay, gRPC, or logging code
- Broad map-schema migrations beyond what is required to support the typed Capture Mode invariant in the current design
