Status: needs-triage

# Reduce existing compiler warnings in hold-packet follow-up

## What to build

Create a follow-up cleanup pass that reduces existing warning noise in the `hold-packet` workspace without changing runtime behavior. This issue is intentionally separate from Option A Anti-loop Guard semantics.

Target warning classes observed during Option A validation runs:

- Unused imports in userspace code paths.
- Unused fields/method warnings in replay structures.
- Other `warn(unused)` diagnostics that are pre-existing and unrelated to Anti-loop Guard behavior.

## Acceptance criteria

- [ ] Option A PR remains behavior-focused and does not include unrelated warning cleanups.
- [ ] A dedicated cleanup PR removes or justifies current warnings in `hold-packet` userspace crates.
- [ ] Cleanup changes preserve behavior; relevant tests continue to pass.
- [ ] If any warnings are intentionally retained, they are documented with rationale in code comments or PR notes.

## Blocked by

None — can start immediately after Option A PR merges.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

- This issue was split out to keep the Anti-loop Guard Option A PR tightly scoped.
- Cleanup should be handled as a dedicated follow-up PR.
