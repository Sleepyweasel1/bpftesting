# Wire agent call in reconcile happy path

Category: enhancement
Status: completed
Type: AFK

## What to build

Implement the hold-operator reconcile path that invokes the hold-packet agent using the agreed control contract. Handle success and failure responses and keep status updates consistent with operator outcomes.

## Acceptance criteria

- [x] Reconcile invokes hold-packet agent on the happy path using the defined control contract.
- [x] Agent success and failure outcomes are translated to clear status updates.
- [x] Recoverable failures are retried through reconcile without breaking idempotency.

## Blocked by

- .scratch/hold-operator-starter-main-loop/issues/02-reconcile-intent-to-status-updates.md
- .scratch/hold-operator-starter-main-loop/issues/03-hold-packet-agent-control-contract-decision.md

## Comments

> *This was generated by AI during triage.*

## Triage Completion

- Recommended category: `enhancement`.
- Reconcile now resolves the target Deployment and same-named Service, derives intent from Deployment replicas, and invokes the hold-packet gRPC agent for `HoldCapture` and `RemoveCapture`.
- Agent success maps to `Succeeded`; repeated already-applied intent maps to `NoOp`; agent lookup and RPC failures map to `RecoverableFailure` with requeue.
- Operator-side retry policy now performs up to three RPC attempts total with 200 ms and 1 s backoff before falling back to controller requeue.
- The operator gRPC client is generated from the shared `holdpacket.proto`; endpoint is configurable via `HOLD_PACKET_GRPC_ADDR` and defaults to `http://127.0.0.1:50051`.
- Validation: `cargo check -p hold-operator` completed successfully.
