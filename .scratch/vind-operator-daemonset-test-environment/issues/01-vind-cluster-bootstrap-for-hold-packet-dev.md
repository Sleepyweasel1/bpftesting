Status: completed
Type: AFK
Category: enhancement

# Vind cluster bootstrap for hold-packet dev

## What to build

Add a repo-owned entrypoint for creating and tearing down a repeatable `vind` Kubernetes environment for hold-packet development. The slice should cover only the base environment needed for later operator and daemon testing: cluster startup, image build or load workflow, and an objective readiness check that confirms the cluster prerequisites are present.

## Acceptance criteria

- [x] `mise run vind-up` creates a vcluster named `hold-packet-dev` using the Docker driver.
- [x] `mise run vind-down` hard-deletes the `hold-packet-dev` cluster; is a no-op if already absent.
- [x] `vcluster` and `kubectl` are declared in `mise.toml` `[tools]` so `mise install` provisions them.
- [x] `vind-up` waits for the node to reach `Ready` state before exiting (smoke check).
- [x] Image build is out of scope for this slice — deferred to issue 02.

## Blocked by

None - can start immediately

## Comments

> *This was generated by AI during triage.*

## Triage Completion

Marked as `completed` after verifying implementation artifacts and behavior:

- `mise run vind-up` and `mise run vind-down` are repo-owned tasks implemented in `mise-tasks/vind-up.sh` and `mise-tasks/vind-down.sh`
- cluster name is fixed to `hold-packet-dev` and both scripts use the Docker driver
- teardown is idempotent when the cluster is absent (`vcluster list` guard in `vind-down`)
- `vind-up` blocks on node readiness via `kubectl wait node --all --for=condition=Ready --timeout=120s`
- `vcluster` and `kubectl` are declared under `[tools]` in `mise.toml`
- image build remains deferred to issue 02, which is tracked and completed separately