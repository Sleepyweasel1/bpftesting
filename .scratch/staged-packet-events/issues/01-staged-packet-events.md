Status: completed

# Emit Staged Packet events from the Staging Area

## What to build

Add an internal notification channel to `Replayer` so that every time `read_and_stage` stores a Staged Packet it sends a `StagedEvent { id: u64, src_ip: IpAddr, dst_ip: IpAddr }` on a `tokio::sync::broadcast` sender. `Replayer::subscribe()` returns a receiver. The existing TAP read loop and staging logic are unchanged; the channel is additive.

End-to-end: a Staged Packet arrives on the TAP device → `read_and_stage` stages it and sends a `StagedEvent` → any subscriber (operator future, metrics, test) receives the event without accessing the internal `staged` map.

## Acceptance criteria

- [x] `Replayer` exposes a `subscribe() -> broadcast::Receiver<StagedEvent>` method.
- [x] `read_and_stage` sends a `StagedEvent` for every successfully staged packet; send errors (no receivers) are silently ignored.
- [x] A unit test subscribes, stages a synthetic frame, and asserts the event carries the correct `id`, `src_ip`, and `dst_ip`.
- [x] `CONTEXT.md` is updated to reference `StagedEvent` as the observable output of the Staging Area.

## Blocked by

None — can start immediately.

## Comments

> *This was generated by AI during triage.*

## Triage Notes

- Classified as `enhancement`.
- Scope and acceptance criteria were already specific and implementation-ready.
- No grilling required because required interfaces, behavior, and tests were fully defined.
- Moved state from `needs-triage` to `ready-for-agent` and immediately executed the scoped implementation.

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Emit `StagedEvent` notifications when packets enter the Staging Area.

**Current behavior:**
`Replayer::read_and_stage` parses/stores packet data in the Staging Area map and returns an ID, but there is no observable stream for other components to react to newly staged packets.

**Desired behavior:**
Each successful staging emits a `StagedEvent { id, src_ip, dst_ip }` over an internal `tokio::sync::broadcast` channel. Consumers subscribe via `Replayer::subscribe()` and can observe staged packets without direct map access. Lack of subscribers must not fail staging.

**Key interfaces:**
- `StagedEvent` — event payload describing staged packet identity and parsed IP endpoints.
- `Replayer::subscribe() -> broadcast::Receiver<StagedEvent>` — observer subscription surface.
- `Replayer::read_and_stage()` — unchanged TAP read contract, now emitting a side-channel event after staging.

**Acceptance criteria:**
- [x] `Replayer` exposes a `subscribe() -> broadcast::Receiver<StagedEvent>` method.
- [x] `read_and_stage` sends a `StagedEvent` for every successfully staged packet; send errors (no receivers) are silently ignored.
- [x] A unit test subscribes, stages a synthetic frame, and asserts the event carries the correct `id`, `src_ip`, and `dst_ip`.
- [x] `CONTEXT.md` is updated to reference `StagedEvent` as the observable output of the Staging Area.

**Out of scope:**
- Converting Staging Area storage away from `HashMap<u64, StagedPacket>`.
- Adding persistence, backpressure handling, or durable event replay for `StagedEvent` consumers.
- Wiring operator business logic to consume events in this slice.

> *This was generated by AI during triage.*

## Completion Notes

- Added `StagedEvent` and a broadcast sender field on `Replayer`.
- Added `Replayer::subscribe()` returning `broadcast::Receiver<StagedEvent>`.
- Refactored staging write path to emit events and ignore send errors when there are no subscribers.
- Added a unit test that subscribes, stages a synthetic IPv4 frame, and verifies `id`, `src_ip`, and `dst_ip` in the received event.
- Updated domain glossary text in `CONTEXT.md` to declare `StagedEvent` as the Staging Area observable output.
