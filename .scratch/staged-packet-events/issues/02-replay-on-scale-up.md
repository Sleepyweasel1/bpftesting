Status: ready-for-agent

# Trigger Replay from staged-packet events after scale-up

## What to build

Wire the Staging Area notification channel into the Reconciler's wake-up path. After a scale-up transition causes `remove_rule`, the Reconciler subscribes to `StagedEvent` notifications from the hold-packet daemon (via a new `WatchStagedPackets` streaming gRPC RPC or by polling `ListRules` and comparing against a local staging cache) and calls `replay_rule(id)` for each Staged Packet belonging to the newly-woken IP. The daemon removes the Staged Packet on successful replay.

End-to-end: a TCP SYN arrives for a held IP → gets staged → Deployment scales back up → Reconciler detects staging event → calls `ReplayRule(id)` → daemon writes frame to TAP → kernel delivers packet to woken pod.

## Acceptance criteria

- [ ] A streaming `WatchStagedPackets` RPC (or equivalent push mechanism) is added to `CapturelistService` proto and implemented in `grpc.rs`, backed by the `Replayer` broadcast channel.
- [ ] `CaptureControl` trait gains a `watch_staged() -> impl Stream<Item=StagedEvent>` method; `HoldPacketClient` and `FakeCaptureControl` implement it.
- [ ] The Reconciler calls `replay_rule` for each `StagedEvent` whose `dst_ip` matches a recently-woken Captured IP.
- [ ] An integration test stages a synthetic frame, triggers a fake scale-up reconcile, and asserts `replay_rule` is called with the correct ID.
- [ ] If `replay_rule` returns "not found" (packet pruned), the error is logged and skipped — not retried.

## Blocked by

- `.scratch/staged-packet-events/issues/01-staged-packet-events.md`
- `.scratch/operator-grpc-adapter/issues/02-reconciler-capture-membership.md`

## Comments

> *This was generated by AI during triage.*

## Triage Notes

- Classified as `enhancement`.
- Scope and acceptance criteria are specific enough for implementation.
- Grilling was not required because key interfaces, behavior, and tests are already defined.
- One prerequisite issue is already complete (`staged-packet-events/01`), while this issue remains blocked on `operator-grpc-adapter/02`.
- Moved state from `needs-triage` to `ready-for-agent` so it can be picked up once blockers clear.

> *This was generated by AI during triage.*

## Agent Brief

**Category:** enhancement
**Summary:** Replay staged packets for recently-woken destinations after scale-up.

**Current behavior:**
After a scale-up transition removes capture rules, staged packets can remain in daemon storage without a control-plane trigger that replays them into TAP for the newly-woken destination.

**Desired behavior:**
The operator should observe staged-packet notifications and invoke `replay_rule(id)` when the staged packet destination matches a recently-woken captured IP. Successful replay removes the staged packet. If replay returns "not found" (already pruned), log and skip without retrying.

**Key interfaces:**
- `CapturelistService` staged-packet watch surface (streaming `WatchStagedPackets` or equivalent push path) — emits staged packet events to consumers.
- `CaptureControl::watch_staged() -> impl Stream<Item = StagedEvent>` — operator-facing abstraction for staged event subscription.
- Reconciler scale-up wake path — tracks recently-woken destination IPs and gates replay calls by `dst_ip` match.
- `replay_rule(id)` error handling — treats "not found" as non-fatal and non-retriable.

**Acceptance criteria:**
- [ ] A streaming `WatchStagedPackets` RPC (or equivalent push mechanism) is added to `CapturelistService` proto and implemented in `grpc.rs`, backed by the `Replayer` broadcast channel.
- [ ] `CaptureControl` trait gains a `watch_staged() -> impl Stream<Item=StagedEvent>` method; `HoldPacketClient` and `FakeCaptureControl` implement it.
- [ ] The Reconciler calls `replay_rule` for each `StagedEvent` whose `dst_ip` matches a recently-woken Captured IP.
- [ ] An integration test stages a synthetic frame, triggers a fake scale-up reconcile, and asserts `replay_rule` is called with the correct ID.
- [ ] If `replay_rule` returns "not found" (packet pruned), the error is logged and skipped — not retried.

**Out of scope:**
- Reworking TAP frame parsing or Staging Area storage internals.
- Adding durable event replay or persistence beyond the current in-process event stream.
- Changing ScaleToZero membership reconciliation logic beyond consuming its wake/sleep outcomes.
